const environment = process.env.NODE_ENV || "development";
const config = require("../../mhh[3~hhhhhhhhhhhh[3~[3~[3~hhhh/knexfile")[environment];
const knex = require("knex")(config);

module.exports = knex;
